// Dynamic loader used for webpack
// We cannot exclude dynamic file paths from webpack.config.
// Instead mark the loader as an ExternalsPlugin in webpack.config
// and then use it to load dynamic require paths
'use strict';
exports.load = function (path) {
    return require(path);
};